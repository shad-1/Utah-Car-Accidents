@{
    @model IEnumerable<string>;
}

@foreach (var county in Model)
{
    @*
            @todo: use checkboxes for style!

        <div class="form-check">
            <input class="form-check-input" type="checkbox" id="@county\-filter" asp-controller="Home" asp-action="Index">
            <label class="form-check-label" for="@county\-filter">@county</label>
        </div>
    *@
    <div>
        <a class="filter-item @(county ==  ViewBag.SelectedCounty ? "active" : "")" asp-controller="Home" asp-action="Index" asp-route-countyegory="@county" asp-route-pageNum="1">@county</a>
    </div>
}
<div class="">
    <a class="my-4" asp-controller="Home" asp-action="Index" asp-route-countyegory="All" asp-route-pageNum="1">Reset</a>
</div>
